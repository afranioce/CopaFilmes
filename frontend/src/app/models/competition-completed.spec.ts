import { CompetitionCompleted } from './competition-completed';

describe('CompetitionCompleted', () => {
  it('should create an instance', () => {
    expect(new CompetitionCompleted()).toBeTruthy();
  });
});
